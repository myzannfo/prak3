# Конфигурационное управление

## 📋 Задание
Разработать ассемблер и интерпретатор для учебной виртуальной машины (УВМ) по варианту №10.

## 🏗️ Структура проекта
```
├── asm.py              # Этап 1: Ассемблер (текстовый → бинарный)
├── vm.py               # Этапы 2-3: Интерпретатор с памятью и АЛУ
├── test_program.asm    # Пример программы на ассемблере
├── program.bin         # Скомпилированная программа
├── dump.csv           # Дамп памяти в CSV формате
└── README.md          # Документация проекта
```

## 📊 Спецификация УВМ (Вариант 10)

### Команды:
- **LOADC const, addr, dest** - загрузка константы в регистр
- **READM offset, src, dest, base** - чтение из памяти
- **WRITEM src, dest, base** - запись в память  
- **POPCNT src, dest, base** - подсчет единичных битов (popcount)

### Тестовые примеры из спецификации:
1. **LOADC** A=1, B=946, C=0 → `0x81 0xEC 0x00 0x00`
2. **READM** A=21, B=57, C=4, D=2 → `0x55 0x0E 0x0A`
3. **WRITEM** A=39, B=4, C=2 → `0x27 0x05`
4. **POPCNT** A=44, B=4, C=7 → `0x2C 0x0F`

## 🚀 Использование

### 1. Установка зависимостей:
```bash
# Требуется только стандартная библиотека Python 3
# Проверьте наличие Python 3
python3 --version
```

### 2. Создание тестовой программы:
Создайте файл `test_program.asm`:
```asm
; Тестовая программа для УВМ вариант 10
LOADC 946 0 0    ; R0 = 946
LOADC 0x100 0 2  ; R2 = 0x100
LOADC 123 0 4    ; R4 = 123
LOADC 0x150 0 3  ; R3 = 0x150
WRITEM 4 3       ; mem[R3] = R4
READM 0x50 3 5 2 ; R5 = mem[R2+0x50]
LOADC 0xFF 0 6   ; R6 = 255
POPCNT 6 7       ; R7 = popcount(R6)
LOADC 0x200 0 1  ; R1 = 0x200
LOADC 42 0 4     ; R4 = 42
WRITEM 4 1       ; mem[R1] = R4
```

### 3. Ассемблирование (Этап 1):
```bash
# Без отладочного вывода:
python asm.py test_program.asm program.bin

# С отладочным выводом промежуточного представления:
python asm.py test_program.asm program.bin test
```

### 4. Запуск интерпретатора:
```bash
# Запуск программы из файла:
python vm.py run program.bin dump.csv 0 100
```

### 5. Тестирование команд:
```bash
# Тест команды POPCNT (Этап 3):
python vm.py test_popcnt

# Тест копирования массива (Этап 2.6):
python vm.py test_array
```
